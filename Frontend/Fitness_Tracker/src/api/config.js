// API Configuration for FitTrack
// Change this to your laptop's IP address (e.g., http://192.168.0.108:5000) 
// to allow mobile testing on the same Wi-Fi network.

const API_BASE_URL = 'http://localhost:5000';

export default API_BASE_URL;
